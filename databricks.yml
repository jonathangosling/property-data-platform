bundle:
  name: property-data-platform

variables:
  s3_bucket:
    description: S3 bucket name for Delta Lake storage
    default: ""

include:
  - resources/*.yml

targets:
  prod:
    default: true
